#! /bin/sh
#GRGEN=`readlink -f "$0"`
GRGEN_PATH="/Users/maneeshyadav/programs/grgen/engine-net-2/bin"
if which mono &> /dev/null; then
	mono "$GRGEN_PATH/GrGen.exe" $*
else
	"$GRGEN_PATH/GrGen.exe" $*
fi
