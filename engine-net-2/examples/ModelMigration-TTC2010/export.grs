# This is the GrGen.NET solution to the Model Migration case of Grabats 2010.

include core.grs

# Export the graph
redirect emit migrated_model.xmi
xgrs (id) = initial_id
xgrs (id) = number_node(id)*
xgrs XMI_prefix
xgrs [emit_activity]
xgrs XMI_suffix
xgrs finish_dump
