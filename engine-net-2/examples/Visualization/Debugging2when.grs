new graph Debugging

debug on match r break if f() && this.i==0
debug on match s continue if f() && this.i==0
debug on match ss continue if f() && this.i==0

debug exec init

debug exec s

clear graph 

debug exec init
new :T($="foobar")

debug exec s

quit
