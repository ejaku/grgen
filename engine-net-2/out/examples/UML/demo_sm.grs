new graph demo_sm

xgrs createGraph

debug xgrs (sm)=findStateMachine && ( (forwardTransitionNoXZ(sm)+|forwardTransitionNoEpsXZ(sm)+|forwardTransition3(sm)+)* | propagateFinalState(sm) | removeEpsilonTransition(sm)* )

#echo "\n\n===========\nEmit graph.\n===========\n\n"

#select actions "lgsp-g2xActions.dll"

#xgrs (cntr)=createAUX_Counter && [uniquifyIDs(cntr)] && deleteAUX_Counter(cntr)

#xgrs toXMI
