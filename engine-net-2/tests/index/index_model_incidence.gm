node class N
{
	i:int = 0;
}

edge class E;

index foo { countIncident(Node) }
index bar { countIncoming(Node) }
index baz { countOutgoing(Node) }

index qux { countIncident(N, E, N) }
index pix { countIncoming(N, E, N) }
index xom { countOutgoing(N, E, N) }

