new graph iteratedArrayAccumulation/iteratedArrayAccumulation

exec init

eval [?t]

eval [?t2]

show num nodes N
show num nodes M

exec (::m)=[r] ;> {::m=::m[0]}

show node m

show num nodes N
show num nodes M

#exec tnested

quit
