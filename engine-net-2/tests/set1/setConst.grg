test tb : (boolean, boolean)
{	
	return( 1 in set<int>{0, 1, 2, 3},
			1 in set<int>{nodes().size(), nodes().size()+1,
							nodes().size()+2, nodes().size()+3} );
}

test tbf : (boolean, boolean)
{	
	return( 42 in set<int>{0, 1, 2, 3},
			42 in set<int>{nodes().size(), nodes().size()+1,
							nodes().size()+2, nodes().size()+3} );
}
