actions incrementer; // using Firm;

rule ReUsedEdges {
	pattern {
		node (x,y,z): Node;
		a: Node;
		b: Node;
		c: Node;
		x -e:Edge-> y; //ok
//		x -e-> y;      //duplicate error
		x -e-> y;      //error
	}
	negative {
		a -e-> y;      //ok
//		x -e-> z;      //error
	}
	replace{
		x -e:Node-> y;      //ok
//		x -e-> z;      //error
	}
}
