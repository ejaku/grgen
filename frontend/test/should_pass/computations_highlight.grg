using model_init;

function foo(n:Node) : string
{
	Debug::highlight(0, "vis=0", n, "n");
	return("muh");
}

procedure bar(n:Node) : (string)
{
	Debug::highlight(0, "vis=0", n, "n");
	return("muh");
}

test t()
{
	independent {
		n:Node;
		if{!n.visited[0];}
		yield {
			Debug::highlight(0, "vis=0", n, "n");
		}
	}
}

rule r()
{	
	n:Node;
	if{!n.visited[0];}
	modify {
		def var vis:int;
		def var res:string = "nNn";
		
		eval { 
			Debug::highlight(0, "vis=0", n, "n");
		}
		eval {
			Debug::highlight(0, "vis=0", n, "n", n, res);
		}
		
		exec ( Debug::highlight(0, "vis=0", n, "n", n, res) );
	}
}
