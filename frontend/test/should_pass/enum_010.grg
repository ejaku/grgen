using enum_model_006;

rule switch {
	goto:struct -float:Edge-> int:struct;

    modify {
        eval {
            goto.for = bool::FileNotFound;
            int.for = bool::else;
        }
    }
}
