using deep_model_001;

rule r(y : Acc) {
  hom(a,b);
  a : A;
  b : B;
  modify{
    c : AcccAccAcA<b>;
    n : Node;   // can never be a Acc
    exec(r(c)); // subtype as param
    exec(r(y)); // exact type as param
    exec(r(n)); // supertype as param
  }
}
