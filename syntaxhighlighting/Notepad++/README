Copy the userDefineLang.xml to %APPDATA%\Notepad++ if there is no userDefineLang.xml existing yet
or merge the two UserLang entries to your already existing userDefineLang.xml
Restart Notepad++ (which must be of version >= 6.3)
