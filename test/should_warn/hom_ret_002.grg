
// should warn, that x is hom with y, and y is deleted, so returned
// node x might be a deleted one

rule r:(Node) {
	pattern {
		x: Node;
		y: Node;
		hom(x, y);
	}
	modify {
		delete(y);
		return(x);
	}
}

